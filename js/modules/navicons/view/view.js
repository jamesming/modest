
define([
		'core',
		'jquery', 
		'underscore', 		
		'backbone',
		'text!modules/navicons/index.html?v='  + (Math.floor(Math.random() * (99999999999999 - 1 + 1)) + 1) ], function(
		
		core,
		$, 
		_, 		
		Backbone, 
		template
		) {
			
		return Backbone.View.extend();

});